import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// React 17 render API — m-next is pinned to React 17 (Emotion + Syncfusion).
ReactDOM.render(<App />, document.getElementById('root'));
