// Stub for MethodUI's state/hooks barrel — typed Redux hooks (useAppDispatch,
// useAppSelector, etc.). Kit demos don't dispatch real actions, so we
// re-export react-redux's plain hooks under common names.

export { useDispatch as useAppDispatch, useSelector as useAppSelector } from 'react-redux';
