// Stub for MethodUI's views/panels/contextual — full-blown contextual panels
// (Help, Search, etc.) that depend on the runtime panel system. In kit we
// render placeholders; the demo doesn't actually open them.

import React from 'react';

export const GlobalHelp = () => <div style={{ padding: 16 }}>Help panel placeholder</div>;
export const GlobalSearch = () => <div style={{ padding: 16 }}>Search panel placeholder</div>;
