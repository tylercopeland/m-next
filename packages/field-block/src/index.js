import FieldBlock from './fieldBlock';

// FormSection is the canonical name (audit-driven rename). The legacy
// FieldBlock default export is kept indefinitely for backwards compatibility.
export const FormSection = FieldBlock;

export default FieldBlock;
