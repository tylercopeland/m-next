import ThemeProvider, { useThemeSwitcher } from './ThemeProvider';
import useTheme from './use-theme';
import composeTheme, { themeNames } from './compose-theme';

export { ThemeProvider, useTheme, useThemeSwitcher, composeTheme, themeNames };
export default ThemeProvider;
