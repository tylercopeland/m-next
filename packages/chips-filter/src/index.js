import ChipsFilter from './ChipsFilter';

// Audit-flagged rename: FilterChips is the canonical name going forward.
// `ChipsFilter` remains as the default export for backwards compatibility.
export { ChipsFilter as FilterChips };
export { ChipsFilter };
export default ChipsFilter;
